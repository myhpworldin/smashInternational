# Copy to .env.local and fill in real values. Never commit .env.local.

# MongoDB Atlas connection string — Cluster → Connect → Drivers
MONGODB_URI=

# Optional — defaults to "smash_international" if unset
MONGODB_DB=
